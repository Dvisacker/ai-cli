{
  "name": "openai-cli",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "ai": "npm run build && node dist/index.js ask",
    "start": "tsc index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Van Isacker",
  "license": "ISC",
  "dependencies": {
    "commander": "^9.4.1",
    "dotenv": "^16.0.3",
    "openai": "^3.1.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/node": "^18.11.15"
  }
}
